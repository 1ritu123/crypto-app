import { makeStyles } from "@mui/styles";
export const useStyles=makeStyles({

    title:{
      flex:1,
      color:'gold',
      fontFamily:"Montserrat",
      fontWeight:'bold',
      cursor:'pointer'
    }
  
  })